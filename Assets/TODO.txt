Roslyn Analyzers
- Ensure positional indexes are unique and ordered
- Ensure optional indexes are unique and ordered
- Ensure command parent type is also a command
- Ensure variadic attributes are only on array types
- Ensure only one variadic per command class
- Ensure only one argument attribute per field

TODO: 
- alias capability
- fix settings
- unity object binding
- inject type adapter registry into adapter base class so complex adapters can use primitive ones.
- add prompt builder to context
- auto complete
- style facelift and conformance
- verify namespaces and TODOs and remove dead usings

To Document:
commands overriding gui
piping
help commands and such
    (how to display parse rules and what not in terminal)
type adapters
bootstrap
command context methods (especially foreach)

Future features:
- Session restoration 
    Allowing editor windows to survive domain reload requires hosts being able to serialize their state
    Likely best as an optional interface
    Will require a stable unique key for each window

- Permissions

- GUI
      
DEV NOTES: 
    - Currently, the graphics host only knows a window has closed when querying it to see if its toggleable.
      This is done because editor windows especially can close from outside sources. I dont think its unclean, just be aware.
    - Not including headless host in design, this is never going to be useful or implemented


